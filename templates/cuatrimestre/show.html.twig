{% extends 'extends/iboxContent.html.twig' %}

{% block title %}Cuatrimestre{% endblock %}

{% block contenidoIbox %}
    <h1>Cuatrimestre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cuatrimestre.id }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ cuatrimestre.numero }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cuatrimestre_index') }}">back to list</a>

    <a href="{{ path('app_cuatrimestre_edit', {'id': cuatrimestre.id}) }}">edit</a>

    {{ include('cuatrimestre/_delete_form.html.twig') }}
{% endblock %}
